# Git commit message rules
When Cursor auto-generates a Git commit message for this repository (including the Source Control "Generate commit message" / sparkle button), follow Conventional Commits, include the current Git branch name, and write the description in Simplified Chinese (简体中文).

## Format
```
<type>(<branch>): <简体中文简述>

[可选正文：说明动机与影响]
```

- `type` 使用英文（小写）
- `scope` 必须填写**当前 Git 分支名称**（如 `git branch --show-current` 的结果），不要省略、不要改写
- 冒号后的简述与正文必须使用简体中文
- 简述不超过约 50 个汉字/72 字符，祈使语气，不加句号
- 只输出一条完整提交信息，不要解释

## type 取值
- `feat`: 新功能
- `fix`: 修复缺陷
- `docs`: 文档变更
- `style`: 不影响逻辑的格式调整
- `refactor`: 重构（非功能、非修复）
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建/工具/杂项
- `ci`: CI 配置
- `build`: 构建系统或依赖
- `revert`: 回滚提交

## Examples
假设当前分支为 `3d_speak_audio_compress`：
```
feat(3d_speak_audio_compress): 支持待初始化前缓存声纹注册请求
fix(3d_speak_audio_compress): 修复 30 秒自动停止时的资源释放
docs(3d_speak_audio_compress): 补充初始化前声纹注册说明
chore(3d_speak_audio_compress): 更新 .gitignore 忽略缓存目录
```
